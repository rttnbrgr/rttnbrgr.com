{
  "name": "rttn",
  "version": "0.0.1",
  "description": "Homepage",
  "main": "index.html",
  "scripts": {
    "start": "parcel src/index.html -d dist --cache-dir .cache",
    "dev": "yarn run start",
    "build": "parcel build src/index.html -d dist --cache-dir .cache",
    "stage": "netlify deploy --dir dist",
    "deploy": "netlify deploy --dir dist --prod",
    "launch": "yarn run build && yarn run deploy"
  },
  "author": "Nathan Mateo Rothenberger (@rttnbrgr)",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "cssnano": "^4.1.10",
    "parcel": "^1.12.4"
  }
}
